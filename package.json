{
  "name": "infrastructure-project",
  "private": true,
  "workspaces": [
    "src/services/*",
    "src/packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}
